# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "jekyll", ENV["JEKYLL_VERSION"] if ENV["JEKYLL_VERSION"]

# Pin specific versions to avoid FactoryBot/CreateList issue
gem "rubocop-factory_bot", "2.23.1"
